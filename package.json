{
  "name": "airforce",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "build": "cd client && node ../node_modules/vite/bin/vite.js build",
    "start": "cd server && npm start"
  }
}
