{
  "name": "airforce",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "postinstall": "cd client && npm install",
    "build": "cd client && npx vite build",
    "start": "cd server && npm start"
  }
}
