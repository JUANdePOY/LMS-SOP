const m = require('./utils/bulkImportValidation.js');
console.log('=== LOADED normalizeQuestion SOURCE ===');
console.log(m.normalizeQuestion.toString());
console.log('=== toCorrectAnswerArray exported? ===', typeof m.toCorrectAnswerArray);
