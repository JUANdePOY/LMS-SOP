const express = require('express');
const router = express.Router();

// TODO: Implement reports API
router.get('/', (req, res) => res.json({ message: 'Reports API - TODO' }));

module.exports = router;