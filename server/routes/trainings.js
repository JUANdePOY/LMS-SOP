const express = require('express');
const router = express.Router();

// TODO: Implement trainings API
router.get('/', (req, res) => res.json({ message: 'Trainings API - TODO' }));

module.exports = router;