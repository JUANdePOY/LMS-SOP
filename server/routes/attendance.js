const express = require('express');
const router = express.Router();

// TODO: Implement attendance API
router.get('/', (req, res) => res.json({ message: 'Attendance API - TODO' }));

module.exports = router;